# NRO Offline Server - Environment Variables Template
# Copy this file to .env in your selfhost directory and configure the values

# ============================================
# User and Permissions Configuration
# ============================================
# Your user ID - find with: id -u
PUID=1000

# Your group ID - find with: id -g
PGID=1000

# Your timezone - find with: timedatectl or ls /usr/share/zoneinfo/
# Examples: America/New_York, Europe/London, Asia/Ho_Chi_Minh, Asia/Bangkok
TZ=Asia/Ho_Chi_Minh

# ============================================
# MySQL Database Configuration for NRO
# ============================================
# MySQL root password - generate with: openssl rand -base64 32
# IMPORTANT: Use a strong, unique password for production
NRO_MYSQL_ROOT_PASSWORD=your_secure_root_password_here

# MySQL user password for nro_user
# IMPORTANT: Use a strong, unique password for production
NRO_MYSQL_PASSWORD=your_secure_user_password_here

# ============================================
# Notes
# ============================================
# 1. Generate secure passwords with: openssl rand -base64 32
# 2. Make sure to update both MySQL passwords
# 3. Keep this file secure and never commit .env to version control
# 4. The database user (nro_user) and database name (nro) are configured in docker-compose.yml
